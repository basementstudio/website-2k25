/**
 * Departments rendered as "the crew". Standalone module (no server imports) so
 * the client-side crew grid, the /people JSON-LD, and /people.md all agree on
 * who the team is.
 */
export const CREW_DEPARTMENTS = ["Management", "Design", "Development"]
